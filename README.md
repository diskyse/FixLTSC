# FixLTSC
FixLTSC is an easy, lightweight tool that provides fixes for common issues on Windows LTSC builds.

## Available fixes
- Disable `ms-gamingoverlay` when launching a game.
- Fix the OneDrive tab not opening in File Explorer.
- On Windows 10, Fix `Open` not being the default on right-click menus on certain file types.
